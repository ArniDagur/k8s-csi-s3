apiVersion: v1
kind: ServiceAccount
metadata:
  name: csi-s3-provisioner-sa
  namespace: {{ .Release.Namespace }}
---
kind: ClusterRole
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: csi-s3-external-provisioner-runner
rules:
  - apiGroups: [""]
    resources: ["secrets"]
    verbs: ["get", "list"]
  - apiGroups: [""]
    resources: ["persistentvolumes"]
    verbs: ["get", "list", "watch", "create", "patch", "delete"]
  - apiGroups: [""]
    resources: ["persistentvolumeclaims"]
    verbs: ["get", "list", "watch", "update"]
  - apiGroups: ["storage.k8s.io"]
    resources: ["storageclasses"]
    verbs: ["get", "list", "watch"]
  - apiGroups: [""]
    resources: ["events"]
    verbs: ["list", "watch", "create", "update", "patch"]
---
kind: ClusterRoleBinding
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: csi-s3-provisioner-role
subjects:
  - kind: ServiceAccount
    name: csi-s3-provisioner-sa
    namespace: {{ .Release.Namespace }}
roleRef:
  kind: ClusterRole
  name: csi-s3-external-provisioner-runner
  apiGroup: rbac.authorization.k8s.io
---
kind: Service
apiVersion: v1
metadata:
  name: csi-s3-provisioner
  namespace: {{ .Release.Namespace }}
  labels:
    app: csi-s3-provisioner
spec:
  selector:
    app: csi-s3-provisioner
  ports:
    - name: csi-s3-dummy
      port: 65535
---
kind: StatefulSet
apiVersion: apps/v1
metadata:
  name: csi-s3-provisioner
  namespace: {{ .Release.Namespace }}
spec:
  serviceName: "csi-provisioner-s3"
  replicas: 1
  selector:
    matchLabels:
      app: csi-s3-provisioner
  template:
    metadata:
      labels:
        app: csi-s3-provisioner
    spec:
      serviceAccount: csi-s3-provisioner-sa
      {{- if .Values.hostNetwork.controller }}
      hostNetwork: true
      dnsPolicy: ClusterFirstWithHostNet
      {{- end }}
      tolerations:
        - key: node-role.kubernetes.io/master
          operator: Exists
        - key: CriticalAddonsOnly
          operator: Exists
        {{- with .Values.tolerations.controller }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: csi-provisioner
          image: {{ .Values.images.provisioner }}
          args:
            - "--csi-address=$(ADDRESS)"
            - "--v=4"
          env:
            - name: ADDRESS
              value: {{ .Values.kubeletPath }}/plugins/ru.yandex.s3.csi/csi.sock
          imagePullPolicy: "IfNotPresent"
          {{- if or .Values.resourceLimits.controller.provisioner.cpuRequests .Values.resourceLimits.controller.provisioner.cpuLimits .Values.resourceLimits.controller.provisioner.memory }}
          resources:
            {{- if or .Values.resourceLimits.controller.provisioner.cpuRequests .Values.resourceLimits.controller.provisioner.memory }}
            requests:
              {{- if .Values.resourceLimits.controller.provisioner.cpuRequests }}
              cpu: {{ .Values.resourceLimits.controller.provisioner.cpuRequests }}
              {{- end }}
              {{- if .Values.resourceLimits.controller.provisioner.memory }}
              memory: {{ .Values.resourceLimits.controller.provisioner.memory }}
              {{- end }}
            {{- end }}
            {{- if or .Values.resourceLimits.controller.provisioner.cpuLimits .Values.resourceLimits.controller.provisioner.memory }}
            limits:
              {{- if .Values.resourceLimits.controller.provisioner.cpuLimits }}
              cpu: {{ .Values.resourceLimits.controller.provisioner.cpuLimits }}
              {{- end }}
              {{- if .Values.resourceLimits.controller.provisioner.memory }}
              memory: {{ .Values.resourceLimits.controller.provisioner.memory }}
              {{- end }}
            {{- end }}
          {{- end }}
          volumeMounts:
            - name: socket-dir
              mountPath: {{ .Values.kubeletPath }}/plugins/ru.yandex.s3.csi
        - name: csi-s3
          image: {{ .Values.images.csi }}
          imagePullPolicy: IfNotPresent
          args:
            - "--endpoint=$(CSI_ENDPOINT)"
            - "--nodeid=$(NODE_ID)"
            - "--v=4"
          env:
            - name: CSI_ENDPOINT
              value: unix://{{ .Values.kubeletPath }}/plugins/ru.yandex.s3.csi/csi.sock
            - name: NODE_ID
              valueFrom:
                fieldRef:
                  fieldPath: spec.nodeName
          {{- if or .Values.resourceLimits.controller.csi.cpuRequests .Values.resourceLimits.controller.csi.cpuLimits .Values.resourceLimits.controller.csi.memory }}
          resources:
            {{- if or .Values.resourceLimits.controller.csi.cpuRequests .Values.resourceLimits.controller.csi.memory }}
            requests:
              {{- if .Values.resourceLimits.controller.csi.cpuRequests }}
              cpu: {{ .Values.resourceLimits.controller.csi.cpuRequests }}
              {{- end }}
              {{- if .Values.resourceLimits.controller.csi.memory }}
              memory: {{ .Values.resourceLimits.controller.csi.memory }}
              {{- end }}
            {{- end }}
            {{- if or .Values.resourceLimits.controller.csi.cpuLimits .Values.resourceLimits.controller.csi.memory }}
            limits:
              {{- if .Values.resourceLimits.controller.csi.cpuLimits }}
              cpu: {{ .Values.resourceLimits.controller.csi.cpuLimits }}
              {{- end }}
              {{- if .Values.resourceLimits.controller.csi.memory }}
              memory: {{ .Values.resourceLimits.controller.csi.memory }}
              {{- end }}
            {{- end }}
          {{- end }}
          volumeMounts:
            - name: socket-dir
              mountPath: {{ .Values.kubeletPath }}/plugins/ru.yandex.s3.csi
      volumes:
        - name: socket-dir
          emptyDir: {}
